# ==========================================
# CBT High School - Environment Configuration
# ==========================================

# Application Port (Exposed via Nginx Reverse Proxy)
APP_PORT=80

# Backend Internal Port
BACKEND_PORT=8080

# PostgreSQL Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USER=cbt_user
DB_PASSWORD=cbt_secret_password_2026
DB_NAME=cbt_db

# Timezone (Default: Asia/Jakarta for WIB)
TZ=Asia/Jakarta

# JWT Secret Key for Authentication Tokens
# Wajib diisi nilai acak minimal 32 karakter. Buat dengan: openssl rand -hex 32
JWT_SECRET=